# Railway / Nixpacks: build and run without a root Dockerfile.
# See https://nixpacks.com/docs/configuration/file

[variables]
NIXPACKS_PYTHON_VERSION = "3.13"

[phases.install]
cmds = ["pip install ."]

[start]
cmd = "python main.py --transport http --host 0.0.0.0"
